import java.util.Scanner;

public class Trie {
	public Node createTrie(Node head) {
		Scanner in = new Scanner(System.in); 
		  
        if(head != null) {
        	System.out.println("Tree is already created");
        	return head;
        }
        else {
        	return head;
        }
	}
}
